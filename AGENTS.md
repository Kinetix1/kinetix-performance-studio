# KINETIX Performance Studio — Agent Notes

All content (contact, pricing, schedule, socials) lives in `src/lib/` config files.
Edit those files to update the site — do not hardcode strings in components.

## TODOs
- Replace `site.socials.instagram` (`#`) with the real Instagram URL
- Add trainer names and certifications to `/about` (photos are in; coaches are unnamed placeholders)
- Confirm free-trial offer terms
- Confirm freeze/pause/refund policy for the FAQ
- Confirm whether GST applies to listed prices
